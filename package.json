{
  "name": "knitwittage",
  "version": "0.0.0",
  "private": true,
  "description": "Knitwittage — a vanilla-JS knitting pattern designer. This package.json exists only for the test suite; the app itself ships as static files.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
